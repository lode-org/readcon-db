% Paste-ready companion wording for the CPC manuscript (readcon-core).
% Main claim remains readcon-core. Do not promote this crate to a second paper.
\texttt{readcon-db} is the companion campaign store: LMDB indexes
(energy, formula, section presence, screening fields) and a derived
RCSO cache over the same CON text files. Text CON remains the
authority. It is not a second CPC paper.
If an ASE.\texttt{db} timing table appears, use Appendix
Table~\ref{tab:fair-campaign} only
(\texttt{examples/benchmarks/ase\_fair\_campaign\_1.json}; fixture
\texttt{tiny\_cuh2.con}; ladder $N=10,50,100,200,500$).
That comparison is measurement-only. ASE.\texttt{db} is not the
product store. Do not substitute the legacy Cu2 stand-in table or a
shorter ladder.

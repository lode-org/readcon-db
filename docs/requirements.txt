# Direct docs dependencies. Pages and CI install the locked set via
# pixi.lock (pixi.toml [feature.docs]). Refresh: pixi lock
sphinx>=7,<9
shibuya>=2026.1.9,<2027
sphinx-copybutton>=0.5.2,<1
sphinx-design>=0.6,<1
myst-parser>=2
